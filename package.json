{
  "name": "dep-score",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "bin": "bun build --compile --minify --sourcemap --bytecode ./src/main.ts --outfile bin",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc --noEmit",
    "build": "tsc"
  },
  "bin": {
    "dep-score": "bin/cli.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/bun": "latest"
  },
  "dependencies": {
    "query-registry": "^4.0.0"
  }
}
